#SERVER CONFIGURATION
server:
  port: ${PORT:6012}
  servlet:
    application-display-name: hourfun-auth

#SPRING CONFIGURATION
spring:
  application:
    name: hourfun-auth
  profiles:
    active: local
  cloud:
    config:
      #http://localhost:6999/hourfun-eureka/local
      uri: http://localhost:6999
      # name: hourfun-eureka